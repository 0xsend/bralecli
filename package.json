{
  "name": "brale-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "nub run -r --filter './packages/*' build && nub run -r --filter './apps/*' build",
    "typecheck": "tsc -b",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "test": "vitest run",
    "check": "nub run typecheck && nub run lint && nub run format:check && nub run test",
    "knip": "knip",
    "jscpd": "jscpd .",
    "spec:refresh": "nub scripts/refresh-spec.ts",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "nub run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@types/node": "^26.4.1",
    "jscpd": "^5.1.2",
    "knip": "^6.34.0",
    "oxfmt": "^0.60.0",
    "oxlint": "^1.81.0",
    "tsdown": "^0.22.14",
    "typescript": "^7.0.2",
    "vitest": "^3.2.7",
    "yaml": "^2.9.0"
  }
}
